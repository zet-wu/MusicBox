{
  "name": "musicbox",
  "version": "0.2.6",
  "description": "A visually stunning, plugin-based, and highly customizable local music player.",
  "main": "dist/main/main.js",
  "author": {
    "name": "asxez",
    "email": "2973918177@qq.com"
  },
  "homepage": "https://github.com/asxez/MusicBox",
  "license": "MIT",
  "private": true,
  "scripts": {
    "install:renderer": "cd src/renderer && npm install",
    "install:rs": "cd native && npm install",
    "build:rs": "cd native && npm run build",
    "build:ts": "tsc -p src/main/tsconfig.json",
    "watch:ts": "tsc -p src/main/tsconfig.json --watch",
    "dev": "npm run build:renderer && npm run build:rs && npm run build:ts && npm run dev:main",
    "dev:main": "chcp 65001 && electron dist/main/main.js --expose-gc",
    "dev:renderer": "cd src/renderer && npm run dev",
    "typecheck:renderer": "cd src/renderer && npm run typecheck",
    "build": "npm run build:python && npm run build:renderer && npm run build:rs && npm run build:ts && npm run build:main",
    "build:main": "electron-builder",
    "build:renderer": "cd src/renderer && npm run build",
    "build:win": "npm run build:python && npm run build:renderer && npm run build:ts && electron-builder --win",
    "build:mac": "npm run build:python && npm run build:renderer && npm run build:ts && electron-builder --mac",
    "build:linux": "npm run build:python && npm run build:renderer && npm run build:ts && electron-builder --linux",
    "build:icons": "electron-icon-builder --input=./docs/images/logo.png --output=build --flatten",
    "build:python": "node scripts/build-python-module.js",
    "clean": "rimraf dist && cd src/renderer && npm run clean"
  },
  "devDependencies": {
    "@types/node": "^22",
    "cross-env": "^10.1.0",
    "electron": "41.2.1",
    "electron-builder": "^26.8.1",
    "rimraf": "^4.1.2",
    "ts-node": "^10.9.2"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "adm-zip": "^0.5.17",
    "chardet": "^2.1.1",
    "electron-icon-builder": "^2.0.1",
    "fuse.js": "^6.6.2",
    "iconv-lite": "^0.7.2",
    "music-metadata": "11.12.3",
    "node-id3": "^0.2.9",
    "node-smb2": "^1.3.5",
    "pidusage": "^4.0.1",
    "webdav": "^5.9.0",
    "typescript": "^6.0.3"
  }
}
